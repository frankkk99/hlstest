"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import AdminShell from "../admin-shell";
import styles from "../admin.module.css";

type Overview = { activeTitles: number; hiddenTitles: number; readyTitles: number; noPlayerTitles: number; brokenTitles: number; unknownTitles: number; movieTitles: number; seriesTitles: number; sourceCount: number; sourceStatus: Record<string, number>; lastCheckedAt: string };
export default function AdminHealthPage() {
  const [data, setData] = useState<Overview | null>(null); const [error, setError] = useState(""); const [loading, setLoading] = useState(true);
  async function load() { setLoading(true); setError(""); try { const response = await fetch("/api/admin/overview", { cache: "no-store" }); const body = await response.json() as { ok?: boolean; error?: string; overview?: Overview }; if (!response.ok || !body.ok || !body.overview) throw new Error(body.error || "อ่านสถานะไม่สำเร็จ"); setData(body.overview); } catch (loadError) { setError(loadError instanceof Error ? loadError.message : "อ่านสถานะไม่สำเร็จ"); } finally { setLoading(false); } }
  useEffect(() => { void load(); }, []);
  return <AdminShell title="Player Health" description="หน้าตรวจสุขภาพ Player แยกจากเครื่องมือทดสอบเดิม เพื่อให้เห็นภาพรวมและเข้าไปแก้เฉพาะรายการที่มีปัญหา">{loading ? <div className={styles.loading}>กำลังตรวจสถานะ...</div> : error ? <div className={styles.error}>{error} <button className={styles.button} type="button" onClick={() => void load()}>ลองใหม่</button></div> : data && <><section className={styles.metricGrid}><article className={styles.metric}><span>PASS</span><strong className={styles.good}>{data.sourceStatus.pass || 0}</strong><small>source ที่ผ่านการตรวจ</small></article><article className={styles.metric}><span>BLOCKED</span><strong className={styles.bad}>{data.sourceStatus.blocked || 0}</strong><small>ถูกต้นทางบล็อก</small></article><article className={styles.metric}><span>ERROR</span><strong className={styles.bad}>{data.sourceStatus.error || 0}</strong><small>เกิดข้อผิดพลาด</small></article><article className={styles.metric}><span>EXPIRED</span><strong className={styles.warn}>{data.sourceStatus.expired || 0}</strong><small>ลิงก์หมดอายุ</small></article><article className={styles.metric}><span>UNKNOWN</span><strong>{data.sourceStatus.unknown || 0}</strong><small>ยังไม่มีผลตรวจชัดเจน</small></article><article className={styles.metric}><span>ไม่มี Player พร้อมใช้</span><strong className={styles.warn}>{data.noPlayerTitles}</strong><small>รายการที่ไม่ควรขึ้นหน้าเว็บ</small></article></section><section className={styles.sectionGrid}><div className={styles.panel}><div className={styles.panelHeader}><div><h2>การคัดกรองหน้าเว็บ</h2><p>ตรวจจาก title-level source ที่มีอยู่ในฐานข้อมูล</p></div><Link className={styles.topLink} href="/admin/catalog?filter=no-player">ดูรายการที่ไม่พร้อม ↗</Link></div><div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>กลุ่ม</th><th>จำนวน</th><th>ผลต่อหน้าเว็บ</th><th>ไปจัดการ</th></tr></thead><tbody><tr><td><span className={`${styles.statusPill} ${styles.good}`}>READY</span></td><td>{data.readyTitles}</td><td>แสดงได้</td><td><Link href="/admin/catalog?filter=ready">เปิดรายการ</Link></td></tr><tr><td><span className={`${styles.statusPill} ${styles.warn}`}>NO PLAYER</span></td><td>{data.noPlayerTitles}</td><td>ไม่แสดง</td><td><Link href="/admin/catalog?filter=no-player">ตรวจรายการ</Link></td></tr><tr><td><span className={`${styles.statusPill} ${styles.bad}`}>BROKEN</span></td><td>{data.brokenTitles}</td><td>ไม่แสดง</td><td><Link href="/admin/catalog?filter=broken">ตรวจรายการ</Link></td></tr><tr><td><span className={styles.statusPill}>UNKNOWN</span></td><td>{data.unknownTitles}</td><td>ไม่แสดง</td><td><Link href="/admin/catalog?filter=unknown">ตรวจรายการ</Link></td></tr></tbody></table></div></div><div className={styles.panel}><div className={styles.panelHeader}><div><h2>ภาพรวมข้อมูล</h2><p>ใช้วางแผนงานซ่อม/นำเข้า</p></div></div><ul className={styles.list}><li><span>หนัง</span><strong>{data.movieTitles}</strong></li><li><span>ซีรีส์</span><strong>{data.seriesTitles}</strong></li><li><span>รายการเปิดใช้งาน</span><strong>{data.activeTitles}</strong></li><li><span>รายการที่ซ่อน</span><strong>{data.hiddenTitles}</strong></li><li><span>ตรวจล่าสุด</span><strong>{new Date(data.lastCheckedAt).toLocaleString("th-TH")}</strong></li></ul></div></section></>}</AdminShell>;
}
